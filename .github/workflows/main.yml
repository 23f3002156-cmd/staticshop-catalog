name: Email Workflow

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 23f3002156@ds.study.iitm.ac.in
        run: echo "Hello, world!"
